# Security

The suite uses synthetic sealed states and delivery frames only. Never run it
against a production tenant or real conversations. Passing conformance verifies
store semantics, not cryptography, database configuration, backup integrity, or
production authorization.
